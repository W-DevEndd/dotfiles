
include-file = ~/.config/polybar/modules.ini
include-file = ~/.config/polybar/color.ini

[bar/main]
width = 100%
height = 20pt
radius = 0

dpi = 120

background = ${colors.base}
foreground = ${colors.text}

line-size = 3pt

border-size = 0pt
border-color = #00000000

padding-left = 1
padding-right = 1

module-margin = 0

separator = ""
separator-foreground = ${colors.disabled}

; font-0 = Sans;2
font-0 = JetBrainsMono Nerd Font:style=Bold:size=12;2

modules-left = app-launcher dash-seperator memory padding cpu
modules-center = xworkspaces xwindow 
modules-right = record-button wlan pulseaudio dash-seperator date

cursor-click = pointer
cursor-scroll = ns-resize

enable-ipc = true

; wm-restack = generic
; wm-restack = bspwm
; wm-restack = i3

; override-redirect = true

; This module is not active by default (to enable it, add it to one of the
; modules-* list above).
; Please note that only a single tray can exist at any time. If you launch
; multiple bars with this module, only a single one will show it, the others
; will produce a warning. Which bar gets the module is timing dependent and can
; be quite random.
; For more information, see the documentation page for this module:
; https://polybar.readthedocs.io/en/stable/user/modules/tray.html
